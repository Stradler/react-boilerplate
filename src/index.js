import React from 'react';
import ReactDOM from 'react-dom';
import './styles/main.less';

class Welcome extends React.Component {
    render() {
        return <h1>Change this</h1>;
    }
}

ReactDOM.render(<Welcome />, document.getElementById('root'));
